name: redux_undo_example
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  redux: ^4.0.0
  redux_undo:
    path: ../
  flutter_redux: ^0.6.0

flutter:
  uses-material-design: true
